{
  "name": "@buddysaradhi/security",
  "version": "0.1.0",
  "private": true,
  "description": "Per `10_Security.md` invariants. Compile-only scaffold — no runtime wire-up until `docs/rfc/security-master.md` sub-RFCs are ratified.",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc -w",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "argon2": "^0.45.0",
    "zod": "^3.24.2"
  },
  "devDependencies": {
    "@types/node": "^26.1.1",
    "typescript": "^7.0.2",
    "vitest": "^4.1.10"
  }
}
